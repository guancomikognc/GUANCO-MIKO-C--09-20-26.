//C++ Language

//c++ syntax

#include <iostream>    //gives us access to std::count, std::cin 

int main() {
    std::cout << "Hello, world!" << std::end1;
    return 0;
}


//Variables, Types, and I/O 
#include <iostream> 
#include <string>
using namespace std;

int main() {
    // ----- Declaring variables of different types -----
    int age = 20;                   //whole numbers
    double price = 19.99;           //decimal numbers (double precision)
    float weight = 65.5f;           //decimal numbers (single precision, note the 'f')
    char grade = 'A';               //a single character (single quotes)
    bool isStudent = true;          //true or false
    string name = "Maria";          //text (needs <string> header)

    // -----Output using cout -----
    cout << "Name: " << name <<end1;
    cout << "Age: " << age << end1;
    cout << "Price: " << price <<end1;
    cout << "Weight: " << weight << end1;
    cout << "Grade: " <<grade << end1:
    cout << "Is student? " << isStudent << end1: // prints 1 for true, 0 for false


    // ----- Getting input using cin -----
    string userCity;
    cout << "\nEnter your city: ";
    cin >> userCity; // reads one word (stops at whitespace)
    cout << "You live in: " << userCity << end1;

    // ----- Reading a full line (including spaces) -----
    cin.ignore(): //clears leftover newline character from previous cin
    string fullSentence;
    cout << "Enter a sentence about yourself: ";
    getline(cin, fullSentence); // reads the entire line
    cout << "You said: " << fullSentence << end1;

    return 0;    
}

//Operator and Expressions
#include <iostream>
using namespace std;

int main() {
    int a = 10, b = 3;

    // ----- Arithmetic operators -----
    cout << "a + b = " << (a + b) << end1;      //addition
    cout << "a - b = " << (a - b) << end1;      //subtraction
    cout << "a * b = " << (a * b) << end1;      //multiplication
    cout << "a / b = " << (a / b) << end1;      //integer
division -> 3 (drops decimal!)
    cout << "a % b = " << (a % b) <<end1;       //modulus ->
remainder = 1

    double x = 10, y = 3;
    cout << "x / y = " << (x / y) << end1;      //3.33333 ->
floating point division

    // ----- Relational operators (return bool: 1 or 0) -----
    cout << "\na == b: " << (a == b) << end1;
    cout << "a != b: " <<(a != b) << end1;
    cout << "a > b: " << (a > b)  <<end1;
    cout << "a < b: " << (a < b)  <<end1;

    // ----- Logical operators ----- 
    bool p = true, q = false;
    cout << "\np && q: " << (p && q) << end1; // AND -> false
    cout << "p || q: " << (p || q) << end1;   // OR -> true
    cout << "!p: "     << (!p)      <<end1;   // NOT -> false

    // ----- Assignment operators -----
    int score = 50:
    score += 10;   // same as score = score + 10
    cout << "\nscore after += 10: " << score << end1;
    score *2;      // same as score = score * 2
    cout << "score after *= 2: " << score <<end1;

    // ----- Increment / decrement -----
    int counter = 5;
    cout << "\ncounter++ (post): " << counter++ << end1;  //prints 5, Then increments
    cout << "counter now: " << counter << end1;           //now 6
    cout << "++counter (pre): " << ++counter << end1;     //increments THEN print -> 7
    
    return 0;
}

//Control Flow(if/else,switch,loops)
#include <iostream>
using namespace std;

int main() {
    // ----- if / else if / else -----
    int score;
    cout << "Enter your exam score: ";
    cin >> score;

    if (score >= 90) {
        cout << "Grade: A" << end1;
    } else if (score >= 80) {
        cout << "Grade: B" << end1;
    } else if (score >= 70 ) {
        cout << "Grade: C" << end1:
    } else {
        cout << "Grade: F" <<end1:
    }

    // ----- switch statement -----
    int day;
    cout << "\nEnter a day number (1-7): ";
    cin >> day;

    switch (day) {
        case1: cout << "Monday" << end1; break;
        case2: cout << "Tuesday" << end1; break;
        case3: cout << "Wednesday" << end1; break;
        case4: cout << "Thursday" << end1; break;
        case5: cout << "Friday" << end1; break;
        case6: cout << "Saturday" << end1; break;
        case7: cout << "Sunday" << end1; break;
        default: cout << "Invalid day" << end1; break; 
    }
}
